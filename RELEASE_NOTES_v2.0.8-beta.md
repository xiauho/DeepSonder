# Novalist v2.0.8-beta

## 本次版本

- 新增 Windows x64 便携版“立即重启并安装”：更新包安全下载并验证后，可由独立的 `NovalistUpdater.exe` 在 Novalist 退出后完成替换并重新启动。
- 每个发布包新增 `package-files.json`，记录全部受管程序文件的路径、大小和 SHA-256；自动安装只处理清单内文件，不会收纳或覆盖小说项目及其他非受管内容。
- 主程序在交接前会再次验证发布清单、ZIP 安全性、包内文件清单及全部文件哈希，并校验当前安装中的独立更新器；复制到事务缓存后的更新器副本也必须与当前受管清单一致。
- 独立更新器会等待当前 Novalist 进程安全退出；若 Windows 无法确认进程状态或等待超时，将停止安装，不会同时启动第二个程序实例。
- 更新器再次核对下载验证状态、ZIP 摘要、目标版本、包内清单和逐文件哈希；解压到暂存目录后还会绑定并复核已验证清单，避免验证与替换之间的内容变化。
- 替换前备份当前版本的全部受管文件，并拒绝覆盖新版新增路径上的非受管内容。新版必须通过 `Novalist.exe --self-test` 才会启动；替换、写入或自检失败时自动恢复旧版本。
- 自动安装前会确认 AI 任务已经结束并保存当前未保存文档。源码工作区、只读目录、缺少独立更新器或受管清单的旧包会继续提供手动安装方案。
- Windows 打包流程现在同时生成主程序目录包、独立单文件更新器和逐文件软件包清单，并分别执行无界面启动自检。
- AI 长任务新增自动文件桥接：在确认本机 Harness 能读取隔离临时任务文件后，将上下文预算从命令行兼容模式的 24000 字符提升到默认 48000 字符；不支持时自动回退。
- 故事雷达新增脱敏的 AI 上下文报告：逐次显示预算占用、历史摘要纳入数量、各资料分区的保留或裁剪状态、实际传输通道与临时文件清理结果；报告不保存提示词或小说正文。
- AI 任务改用专属上下文配置：状态更新不再读取无关设定，修复任务依据问题证据筛选资料；默认安全模式通过本地标题、角色名、小标题、别名和标签匹配排列相关设定，并提供 `legacy_all` 兼容回退。
- 上下文报告新增资料筛选统计和原因，区分未匹配排除、分类容量排除与最终预算排除；全局核心、作者标记为核心及本次手选体系作为必需资料保护，无法容纳时停止调用并给出提示。

## 从 v2.0.7-beta 升级

公开发布的 `v2.0.7-beta` 已支持安全下载和验证更新包，但自身没有独立更新器和受管文件清单，因此无法自动替换为本版本。

1. 在 `v2.0.7-beta` 中打开“帮助 → 检查更新”，选择“安全下载更新”；也可以直接从 GitHub Release 下载以下三个文件：
   - `Novalist-v2.0.8-beta-windows-x64.zip`
   - `SHA256SUMS.txt`
   - `release-manifest.json`
2. 等待校验完成，或使用 `SHA256SUMS.txt` 手动核对 ZIP 的 SHA-256。
3. 关闭 Novalist，将 ZIP 完整解压到新的可写目录，再运行其中的 `Novalist.exe`。

不要只覆盖单个 EXE；独立更新器、`package-files.json` 和 `_internal` 运行目录必须来自同一个完整发布包。用户配置、小说项目、虚拟环境和更新缓存不会写入发布包。

安装 `v2.0.8-beta` 后，后续满足兼容要求的版本才能使用“立即重启并安装”完成自动升级。

## 预览版限制

- 自动安装目前只支持无需管理员权限且目录可写的 Windows x64 便携包；不提供系统级安装器、服务、开始菜单快捷方式或自动卸载流程。
- 发布清单尚未使用独立 Ed25519 密钥签名；当前信任边界依赖 GitHub HTTPS、仓库 Release 权限、GitHub 资源摘要和本机验证状态。
- 程序尚未进行 Windows Authenticode 代码签名，首次运行时 Windows 可能显示来源或安全提示。
- 本版本无法从自身验证真实的下一版本公开升级；完整的用户端自动替换流程需在后续兼容 beta 发布时继续验证。
- 本版本仍依赖用户自行安装和配置的 DSh / DeepSeek Harness；AI 输出和对项目内容的修改均需人工审核。
- 本版本为公开 beta 预览版，正式写作前仍建议保留独立备份。
