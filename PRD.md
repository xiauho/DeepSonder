# DeepSonder 产品范围与验收原则

本文件描述当前 **DeepSonder-PySide6** 桌面系列的范围。产品展示名称为 **DeepSonder**；程序、包、诊断及发布标识使用 **DeepSonder-PySide6**。具体能力状态以 [功能清单](docs/feature-status.json) 和实际实现为准。

## 当前产品定位

面向单人长篇小说创作，将章节、故事资料、故事记忆、一致性检查和 AI 辅助写作组织在本地项目中。PySide6 系列独立开发、测试和发布。

## 当前使用流程

1. 新建或打开本地项目；旧 Novalist 项目通过只读导入创建独立的新项目。
2. 编辑章节、大纲、人物和世界观等资料；通过目录、快速查找与命令搜索导航。
3. 主动运行按章纲生成正文、续写、选区扩写、文风审校、一致性检查或记忆任务。
4. 查看任务状态与结果，人工审阅后决定采用或放弃。正文修改可撤销，保存和持久化写入遵守各自确认流程。
5. 按需导出 Markdown 或纯文本，配置主题、写作及 AI 参数。

正文创作与审校的具体边界见 [使用说明](docs/writing-style-review.md)。启动与快捷键见 [README](README.md)。

## 数据与交互验收原则

- 用户能识别当前项目、文档、保存状态和 AI 任务状态。
- 保存失败、外部修改和待处理 AI 结果不能被静默覆盖；结果采用时核对来源版本与上下文。
- 任务可取消，生成结果与正式写入分开；用户确认前不把 AI 候选当作已采用事实。
- 旧项目导入只读取名称、作者和有序章节正文，不修改源项目，不复制旧派生数据。
- 本地项目和设置保持独立，只有主动 AI 操作或连接测试才调用外部引擎。详见 [隐私说明](PRIVACY.md)。
- 界面变更在窄桌面宽度、浅色/深色主题及提高显示缩放时验证；不将旧规划中的布局承诺作为已实现能力。

## 当前边界与待评估能力

当前系列没有自动更新或跨系列升级协议。云账户、多人协作、订阅计费与 Web 云服务不属于当前交付范围。

仓库保留的关系图谱服务、结构化抽取、知识重建、评估工具及 schema-v2 后端尚待复用评估；这些模块的存在不代表具有完整的当前桌面入口。本轮不更改它们的数据格式或接入状态。

旧 Web 化工作台规划见 [历史 PRD](docs/archive/planning/desktop-workbench-v1.md)，其中的设想、路由和验收目标不作为当前产品承诺。

## 交付依据

变更同步维护当前文档与功能清单，并通过适用测试；发布还需独立构建和打包程序自检。具体流程见 [贡献指南](CONTRIBUTING.md) 和 [打包说明](PACKAGING.md)。[历史功能对照](docs/feature-parity.json) 不作为当前发布门槛。
