# Novalist 前端产品需求文档（PRD｜SaaS 风格）

> 文档版本：V1.0
> 产品阶段：MVP / Web 化界面规划
> 依据：当前 Novalist 桌面端功能、现有三栏工作台与视觉主题
> 说明：本文中的“SaaS”主要指清晰、模块化、状态可见的产品界面语言，不默认改变“内容本地优先、AI 由用户主动触发”的产品原则。

## 1. 产品概述

 Novalist 是一款面向长篇小说创作者的 AI 写作工作台。产品将章节正文、故事大纲、角色与世界观资料、时间线、章节摘要和长期故事记忆集中在同一项目中，并通过 AI 扩写、AI 续写、一致性检查和记忆更新，降低长篇创作中的上下文切换与设定遗忘成本。

### 1.1 产品目标

- 让作者可以在一个连续工作流内完成“规划—写作—检查—沉淀记忆”。
- 在不打断写作心流的前提下，让相关设定与故事状态随当前章节出现。
- 让 AI 的输入范围、运行状态和输出结果始终可见、可控、可复核。
- 用专业 SaaS 工作台的信息层级提升复杂项目的可管理性，同时保留安静、编辑感强的写作体验。

### 1.2 核心价值主张

“不仅帮你续写，更帮助你的长篇故事始终记得自己是谁。”

### 1.3 MVP 边界

MVP 聚焦单人小说创作、项目与资料管理、Markdown 正文编辑、AI 辅助、一致性检查、故事记忆和整书导出。多人协作、公开作品社区、在线投稿、复杂版本分支、订阅计费与模板市场不纳入当前核心范围。

## 2. 目标用户

### 2.1 核心用户：长篇网络小说作者

- 持续创作数十至数百章，需要管理大量人物、设定和伏笔。
- 熟悉基础文字编辑工具，但不希望维护复杂知识库。
- 希望使用 AI 提升效率，同时重视作品控制权和输出可审查性。
- 核心诉求：快速进入写作、减少设定冲突、记住长期线索、方便整书导出。

### 2.2 次级用户：类型小说与系列故事作者

- 创作科幻、奇幻、悬疑等设定密集型作品。
- 更依赖世界观、能力体系、时间线和角色关系资料。
- 核心诉求：资料与章节联动、当前上下文可视、一致性风险提示。

### 2.3 潜在用户：AI 辅助写作尝试者

- 有故事想法，但缺少结构化推进和长文本管理经验。
- 核心诉求：低门槛项目模板、清晰下一步提示、可控的 AI 生成过程。

## 3. 核心使用场景

1. **创建并初始化小说项目**：填写书名与存储位置，由系统创建总大纲、第一章、角色、世界观、时间线和记忆目录。
2. **日常章节写作**：从项目树选择章节，在编辑器写作；系统自动保存并实时展示字数、段落、阅读时长与光标位置。
3. **写作前查看上下文**：选中章节后，故事雷达自动聚合本章目标、识别到的角色、相关设定和前章摘要。
4. **AI 创作**：用户可选择扩写完整章节或从现有正文末尾续写，确认可能发送的数据范围，查看运行进度，审核结果后选择替换、追加、复制或放弃。
5. **一致性检查**：系统对正文与角色卡、世界观、时间线和长期记忆进行比对，以分级问题列表呈现冲突和修改建议。
6. **更新故事记忆**：完成章节后生成摘要，更新当前位置、人物状态和未回收伏笔；用户确认后写入长期记忆。
7. **查找与资料维护**：跨章节、角色与设定检索，快速创建或编辑资料条目。
8. **专注写作与导出**：隐藏导航、故事雷达和 AI 记录进入专注模式；完成阶段性创作后导出 Markdown 或纯文本全书。

## 4. 全局信息架构

### 4.1 一级导航

- 项目：首页与最近项目
- 写作台：章节正文创作与 AI 操作
- 故事资料：大纲、角色、世界观、能力体系、时间线
- 故事记忆：章节摘要、人物状态、未回收伏笔
- 检查报告：历史一致性检查与问题处理状态
- 导出：整书预览与格式设置
- 设置：写作、DeepSeek Harness、外观、隐私与数据

### 4.2 工作台全局框架

- 顶栏：品牌、当前项目、全局搜索、AI 引擎状态、主题切换、设置入口。
- 操作栏：新章节、保存、导出、专注模式、检查设定、更新记忆，以及包含扩写和续写的“AI 创作”菜单。
- 左侧导航：项目资料树与项目内搜索。
- 中央主区：文档标题、保存状态、正文编辑器、写作统计。
- 右侧故事雷达：上下文、记忆、报告三个页签。
- 底部任务抽屉：AI 请求过程、结果、错误与历史记录。

## 5. 页面清单与模块结构

### 5.1 项目首页 `/projects`

**目的**：让用户快速继续最近创作或创建/导入项目。

**模块结构**

1. 顶部欢迎区：问候语、最近一次写作时间、主按钮“新建项目”。
2. 最近项目卡片：封面占位、项目名、章节数、总字数、最近编辑时间、写作进度。
3. 快速入口：打开本地项目、从示例开始、导入现有 Markdown。
4. 最近动态：最近编辑章节、最近 AI 任务、待处理一致性问题。
5. 数据与隐私提示：本地存储状态、AI 仅在主动调用时处理所需上下文。

**主要操作**：创建、打开、重命名、定位存储目录、移出最近列表。

### 5.2 新建/导入项目 `/projects/new`

**目的**：以最低配置完成项目初始化。

**模块结构**

1. 步骤指示：基本信息 → 初始结构 → 完成。
2. 基本信息：书名、简介、类型（可选）、本地存储位置。
3. 初始结构：创建第一章、总大纲、角色库、世界观、时间线；默认全部选中。
4. AI 设置：暂不配置 / 使用已连接引擎；明确说明不在项目中保存密钥。
5. 完成页：项目创建结果、进入写作台、查看示例资料。

### 5.3 写作台 `/projects/:projectId/write/:documentId`

**目的**：承载最高频的沉浸写作与 AI 辅助闭环。

**模块结构**

1. **项目导航栏**
   - 项目名称与切换入口。
   - 项目内搜索。
   - 分组树：总大纲、章节、角色、世界观、能力体系、时间线。
   - 分组数量、新建入口、展开/收起、拖拽排序（章节优先）。
2. **文档头部**
   - 文档标题与路径。
   - “已保存 / 未保存 / 保存失败”状态。
   - 更多菜单：重命名、复制、删除、在文件夹中显示。
3. **正文编辑器**
   - Markdown 纯文本编辑。
   - 查找与替换。
   - 可选章节结构提示：“## 大纲”“## 正文”。
   - 撤销/重做、自动保存、切换文档前保存。
4. **写作统计栏**
   - 字数、段落、预计阅读时长、光标行列。
   - Markdown 类型提示与自动保存时间。
5. **故事雷达**
   - 上下文：本章目标、出场角色、世界观接入状态。
   - 记忆：当前位置、人物追踪数、本章摘要、未回收伏笔。
   - 报告：一致性问题数量、级别、定位与处理状态。
6. **AI 操作区**
   - AI 扩写、AI 续写、检查设定、更新记忆四个主任务。
   - 任务抽屉展示输入范围摘要、阶段进度、结果和耗时。
   - 扩写结果操作：替换当前正文、复制、重新生成、放弃；续写结果操作：追加到正文、复制、放弃。
7. **专注模式**
   - 隐藏顶栏、操作栏、左右侧栏与任务抽屉，仅保留编辑器和轻量退出提示。

### 5.4 故事资料 `/projects/:projectId/canon`

**目的**：集中维护会被写作和 AI 引用的结构化故事事实。

**模块结构**

1. 二级导航：总大纲、角色、世界观、能力体系、时间线。
2. 条目列表：名称、类型、最近更新、被引用章节数；支持搜索与筛选。
3. 条目编辑区：Markdown 正文、保存状态、基础元信息。
4. 关联信息：出现章节、相关角色/设定、潜在冲突。
5. 新建条目抽屉：选择类型、填写名称、使用对应模板初始化。

### 5.5 故事记忆 `/projects/:projectId/memory`

**目的**：让用户理解和维护 AI 可使用的长期故事状态。

**模块结构**

1. 概览指标：已生成摘要章节数、追踪角色数、未回收伏笔数、最近更新时间。
2. 章节摘要时间线：按章节浏览摘要，支持定位正文与重新生成。
3. 当前故事状态：当前位置、时间点、人物状态、关键物品/关系。
4. 伏笔清单：内容、首次出现章节、状态、计划回收章节、关联人物。
5. 变更预览：AI 更新记忆前后差异，用户确认后写入。

### 5.6 检查报告 `/projects/:projectId/reports`

**目的**：集中管理一致性风险，而不是把检查结果停留在一次性文本输出中。

**模块结构**

1. 报告概览：严重、警告、建议数量及最近检查时间。
2. 筛选栏：章节、问题级别、问题类型、处理状态。
3. 问题列表：问题摘要、正文证据、冲突设定来源、修改建议。
4. 详情侧栏：完整上下文、跳转正文、跳转设定、标记已解决/忽略。
5. 历史记录：每次检查的时间、范围、结果与失败原因。

### 5.7 导出 `/projects/:projectId/export`

**目的**：在导出前确认章节范围、顺序和文件格式。

**模块结构**

1. 格式选择：Markdown、纯文本。
2. 内容范围：全书或指定章节；章节拖拽排序。
3. 导出选项：是否包含书名、目录、章节大纲、分隔符。
4. 预览区：前若干页/章节的合并效果与预计字数。
5. 结果区：保存位置、导出进度、成功后打开文件或所在目录。

### 5.8 设置 `/settings`

**目的**：集中配置写作体验、AI 连接、视觉主题和隐私偏好。

**模块结构**

1. 写作：自动保存开关、保存间隔、正文字号。
2. AI 引擎：命令、启动参数、运行配置、超时、连接测试。
3. 外观：浅色蓝白/深色蓝黑、界面字号、编辑器字号；颜色由主题令牌统一管理。
4. 数据与隐私：本地项目位置、AI 数据范围说明、首次调用告知重置。
5. 快捷键：快捷键列表、冲突提示；MVP 可仅查看。
6. 恢复默认：仅恢复当前分区，避免误清除连接配置。

## 6. 关键交互要求

### 6.1 保存与切换

- 输入后立即显示“未保存”，自动保存成功后切换为“已保存 · 刚刚”。
- 切换文档、关闭项目或退出应用前自动尝试保存；失败时阻止无提示离开。
- 保存失败需保留编辑内容，并提供“重试”和“另存为”。

### 6.2 项目树与搜索

- 单击打开条目；当前条目持续高亮。
- 搜索应覆盖章节名、角色名、设定名，并高亮匹配文本。
- 搜索无结果时保留新建入口和清除条件按钮。
- 删除、批量移动等高风险操作必须二次确认，并清楚说明影响对象。

### 6.3 AI 任务

- 首次调用前展示数据处理告知：将发送哪些资料、由哪个外部引擎处理、结果需人工审核。
- 任务运行时禁用同类重复提交，但不锁死正文编辑；允许取消任务。
- 明确区分“AI 已生成”和“内容已写入正文/记忆”，任何写入都应由用户确认。
- 任务结束使用轻提示反馈；涉及长结果时自动展开任务抽屉，但不抢夺编辑器焦点。
- 错误信息使用用户可理解的原因分类：未连接、超时、权限/额度、内容过长、未知错误，并给出下一步。

### 6.4 故事雷达

- 随当前章节切换自动刷新，刷新期间保持上一次内容并显示局部加载态，避免面板闪空。
- 点击角色、设定或问题可打开详情；从详情返回时保持编辑光标和滚动位置。
- 报告问题支持“定位正文”，在编辑器中短暂高亮对应段落。

### 6.5 响应式与面板行为

- 推荐桌面宽度 ≥ 1280 px；1024–1279 px 自动收窄两侧栏。
- 宽度 < 1024 px 时，左右栏改为覆盖式抽屉，正文保持优先。
- 面板宽度、展开状态、主题和专注模式偏好应持久化。
- 所有图标按钮必须有文字提示；核心操作保留文字标签，避免纯图标猜测。

### 6.6 键盘与无障碍

- 保留现有核心快捷键：保存、查找、专注、AI 扩写、检查、更新记忆、显示/隐藏面板、导出。
- 焦点顺序符合视觉顺序；弹窗打开后焦点被约束在弹窗内，关闭后回到触发控件。
- 不能只用颜色表达保存、错误、警告和 AI 状态；同时使用文字与图标。
- 正文与背景对比度、交互文字与背景对比度满足 WCAG AA 基本要求。

## 7. 空状态设计

| 场景 | 标题 | 辅助说明 | 主操作 |
|---|---|---|---|
| 尚无项目 | 开始你的第一部长篇故事 | 创建项目后可统一管理章节、设定和故事记忆 | 新建项目 |
| 最近项目为空 | 这里还没有最近项目 | 打开已有目录，或使用示例快速体验 | 打开项目 |
| 项目无章节 | 还没有章节 | 创建第一章，开始搭建故事主线 | 新建章节 |
| 编辑器未选中文档 | 选择一份资料开始 | 从左侧打开章节或新建内容 | 新建章节 |
| 角色/设定为空 | 建立第一张角色卡 | 完善资料后，AI 才能更准确地保持设定一致 | 新建角色/设定 |
| 搜索无结果 | 没有找到匹配内容 | 尝试更短的关键词或清除筛选 | 清除搜索 |
| 尚无章节摘要 | 本章还没有摘要 | 完成章节后更新故事记忆 | 生成摘要 |
| 无未回收伏笔 | 暂无未回收伏笔 | 新线索会在记忆更新后出现在这里 | 返回写作 |
| 无检查报告 | 尚未检查本章设定 | 检查角色、世界观与时间线是否一致 | 检查设定 |
| 检查无问题 | 本次未发现明显冲突 | 结果仅供辅助，发布前仍建议人工审读 | 返回写作 |
| AI 未配置 | AI 引擎尚未连接 | 配置本机 dsh 后可使用扩写、检查和记忆功能 | 前往设置 |
| AI 记录为空 | 暂无 AI 工作记录 | 主动发起任务后，过程与结果会显示在这里 | AI 扩写 |

空状态应包含简洁插画或图标、1 行标题、最多 2 行说明和一个明确主操作，不使用大面积营销文案干扰工作区。

## 8. 加载与反馈状态

### 8.1 页面级加载

- 项目首页首次加载使用项目卡片骨架屏，避免整页转圈。
- 打开项目时显示“正在读取项目结构”，并分阶段恢复导航树、当前文档和故事雷达。
- 加载超过 8 秒显示解释与取消入口；失败后保留“重试”和“选择其他项目”。

### 8.2 局部加载

- 切换文档：编辑器使用轻量骨架或保留上一页并加遮罩，载入完成后恢复滚动位置。
- 故事雷达：页签内局部骨架，不阻塞编辑器。
- 搜索：输入后 200–300 ms 防抖；加载时在输入框尾部显示小型进度指示。
- 导出：显示当前阶段和进度，如“合并章节 12/48”“正在写入文件”。

### 8.3 AI 长任务加载

- 固定展示任务名、运行时长、当前阶段和取消按钮。
- 推荐阶段文案：准备上下文 → 调用 AI 引擎 → 整理结果 → 等待用户确认。
- 不展示虚假百分比；无法计算时使用不定进度条。
- 用户收起任务抽屉后，顶栏 AI 状态仍展示运行中；完成时提供非阻塞通知。

### 8.4 操作反馈

- 保存、复制、插入成功：轻量 Toast，2–3 秒自动消失。
- 创建、重命名：就地更新并将焦点移至新条目。
- 删除、覆盖记忆、放弃未保存内容：确认弹窗。
- 连续失败：在当前模块内显示可恢复错误，不只依赖瞬时 Toast。

## 9. 视觉风格

### 9.1 风格关键词

**专业 SaaS、沉浸编辑、安静克制、编辑部气质、浅色蓝白优先、低对比层级、模块化卡片、清晰状态、轻量科技感、可信 AI、内容优先。**

### 9.2 视觉原则

- 以浅色蓝白主题为默认：淡蓝页面背景、白色面板和略亮编辑区，保持长时间写作的清晰度；同时提供蓝黑深色主题。
- 蓝色作为主要强调色，用于主按钮、选中态、焦点与链接；绿色仅表达连接正常或成功；琥珀/红色用于风险。
- 边框替代重阴影，通过 1 px 低对比描边区分导航、编辑区和检查面板。
- 卡片圆角建议 8–12 px，按钮 6–8 px；避免过度圆润和消费化视觉。
- 标题和界面使用现代无衬线中文字体；正文优先使用适合长文阅读的中文字体，并允许用户调节字号。
- 图标采用统一线性风格；AI 能力可使用星芒符号，但不使用大面积渐变或炫光。
- 信息密度中高但留白稳定：工作台紧凑，首页与设置页更宽松。

### 9.3 建议设计令牌

| 令牌 | 深色主题 | 浅色主题 | 用途 |
|---|---|---|---|
| 页面背景 | `#0F1726` | `#F4F8FD` | 全局底色 |
| 面板背景 | `#162235` | `#FFFFFF` | 导航、顶栏、卡片 |
| 编辑区背景 | `#101B2D` | `#FBFDFF` | 正文与输入区 |
| 边框 | `#2A3A52` | `#D9E4F2` | 区域分隔 |
| 主文字 | `#E8F0FA` | `#17253A` | 标题与正文 |
| 次文字 | `#94A8C2` | `#63748A` | 提示与元信息 |
| 主强调 | `#61A8FF` | `#2F80ED` | 主操作、选中、焦点 |
| 成功 | `#7BD39B` | `#278457` | 保存与连接正常 |

## 10. 组件与状态规范

- **按钮**：主按钮每个操作区最多一个；支持默认、悬停、按下、禁用、加载五态。
- **输入框**：包含默认、聚焦、填写、错误、禁用；错误信息紧邻字段展示。
- **项目树条目**：默认、悬停、选中、编辑、拖拽、未保存、加载失败。
- **状态徽标**：已保存、未保存、AI 就绪、AI 运行中、AI 异常、报告严重级别。
- **任务抽屉**：空、运行、成功待确认、失败、已取消。
- **Toast**：成功、提示、警告、错误；错误若需要处理必须同时在页面中保留。
- **弹窗/抽屉**：用于新建、确认、AI 数据告知和详情；禁止多层弹窗叠加。

## 11. 非功能需求

- 常规项目（约 300 章）打开后，目录树应可在 2 秒内可交互；长列表采用虚拟滚动或延迟渲染。
- 编辑输入反馈目标 < 50 ms；自动保存不得造成编辑器卡顿或光标跳动。
- AI 调用必须异步，不冻结正文编辑；超时可配置，默认 600 秒。
- 内容默认保存在用户选择的本地目录；界面不得暗示已完成云端备份。
- 不在项目文件、日志或前端存储中保存 AI 服务密钥。
- 页面刷新、应用异常或任务失败后，优先恢复未提交草稿和用户工作位置。

## 12. MVP 优先级

### P0：首发必须

- 项目首页与新建/打开项目。
- 三栏写作台、章节与资料树、Markdown 编辑、搜索、保存与自动保存。
- 故事雷达的上下文/记忆/报告页签。
- AI 扩写、一致性检查、记忆更新及数据处理告知。
- 专注模式、深浅主题、整书 Markdown/TXT 导出。
- 本文列出的关键空状态、加载状态与错误恢复。

### P1：首发后增强

- 独立故事资料页、故事记忆页、检查报告页。
- AI 结果差异预览、问题处理状态、章节拖拽排序。
- 项目首页写作统计、最近动态和草稿恢复中心。

### P2：后续探索

- 云同步与多设备、协作评论、版本历史、模板市场、订阅与用量管理。
- 这些能力涉及账户体系、权限、计费和隐私策略，需要单独立项，不应由“SaaS 风格”直接推导进入 MVP。

## 13. 核心验收指标

- 新用户从进入产品到创建第一章并开始输入不超过 3 分钟。
- 最近用户从首页恢复上次编辑位置不超过 2 次点击。
- 用户始终能识别当前文档、保存状态、AI 引擎状态与正在执行的任务。
- 任意 AI 输出写入正文或长期记忆前均有明确用户动作。
- AI 任务失败、保存失败与项目加载失败均提供可执行的恢复路径。
- 在 1024 px 桌面宽度下仍可完成选章、写作、查看故事雷达和发起 AI 任务。

## 14. 待产品确认事项

1. “SaaS”仅指视觉/交互语言，还是下一阶段要建设真正的 Web 云服务？
2. Web 版是否继续坚持本地目录为主，或引入账户、云项目和跨设备同步？
3. AI 扩写结果是先进入审核抽屉，还是允许默认直接替换正文？本文建议前者。
4. 一致性报告是否需要跨章节持久化问题状态？本文将其列为 P1。
5. 首发是否需要章节拖拽排序、回收站和版本历史？其中回收站优先级应高于复杂版本分支。
