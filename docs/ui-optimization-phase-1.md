# PySide6 界面优化：第一轮

日期：2026-09-16

## 已实现

- 主导航由 236 px 收窄为 76 px，保留图标、短文字、完整提示和键盘焦点。
- 写作/资料页移除重复页面标题及全局操作栏，操作合并到文档标题区。空间不足时分成两行，避免标题被挤没。
- 新建章节留在目录；项目菜单提供新建、打开、旧版导入、导出和回收站。原文件菜单及快捷键保留。
- 保存、专注、目录和助手开关直接可见。AI 菜单集中扩写、续写、检查、记忆更新与工作记录，各操作沿用原有可用条件。
- 故事雷达改称写作助手，默认收起。面板开关与快捷键状态同步。
- 当前会话内保持侧栏宽度与显隐状态，跨页面和专注模式恢复布局；AI 工作记录不会在专注期间自动展开。
- 文件路径移至标题提示，保存失败仍在文档标题下显示，保存成功后收起。
- AI 状态移至底部状态栏，写作页始终可见。

## 验证

- unittest discover：557 项，4 项跳过，整体通过。
- 新增/扩展测试覆盖窄窗口、操作区换行、面板恢复、快捷键同步、专注期间日志、保存错误与菜单可达性。
- 150% Qt 缩放下的完整窗口和导航测试：6 项通过。
- Qt offscreen 渲染检查：1100×720、1366×768；浅色、深色与 150% 缩放。
- 预览使用演示资料与内存中的示例正文；没有修改用户项目。截图位于 build/ui-review/，不纳入发布输入。

## 范围与后续

本轮完成写作界面框架及面板状态修正。章节/资料切换、统一 AI 任务结果面板、跨应用重启的布局持久化属于后续工作；当前不会声称完整设计方案全部实现。

按用户指定读取 E:/AgentSpace/deepsonder-electron 的前端作为参考。原契约目录 D:/GitHub-store/DeepSonder 不存在；参考目录本身使用独立产品维护规则。本轮未修改 Electron 应用代码，其等价行为和测试在两份 feature-parity.json 中记录为 pending。

## 后续规则调整

2026-09-16：根据用户的新决定，两系列改为独立维护。上述 parity/pending 记录仅保留历史意义，不再构成同步义务或发布阻塞；当前以 AGENTS.md 和 docs/feature-status.json 为准。
